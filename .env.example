# SafarBuddy V2 鈥� environment variable reference. # # Copy this file to `.env.local` and fill in real values. Never commit # `.env`, `.env.local`, or any `.env.*.local` file 鈥� see .gitignore. # # Variable names below are sourced directly from the validated schema in # src/lib/config/env.ts. Do not add a variable here that isn't already # validated there. # --- Node --- NODE_ENV=development # --- Database (Drizzle / Postgres, used by src/db) --- DATABASE_URL= # --- Supabase --- NEXT_PUBLIC_SUPABASE_URL= NEXT_PUBLIC_SUPABASE_ANON_KEY= SUPABASE_SERVICE_ROLE_KEY= # --- Cashfree (payments) --- CASHFREE_APP_ID= CASHFREE_SECRET_KEY= CASHFREE_API_VERSION=2023-08-01 NEXT_PUBLIC_CASHFREE_ENV=sandbox # --- Email --- RESEND_API_KEY= EMAIL_FROM=noreply@safarbuddy.com GMAIL_USER= GMAIL_APP_PASSWORD= ADMIN_NOTIFICATION_EMAIL= # --- Google OAuth --- GOOGLE_CLIENT_ID= GOOGLE_CLIENT_SECRET= # --- AI --- OPENAI_API_KEY= # --- Client-side app config --- NEXT_PUBLIC_APP_NAME=SafarBuddy NEXT_PUBLIC_APP_URL=http://localho
